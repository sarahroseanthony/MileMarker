# MileMarker — Environment Variables
#
# This app is fully client-side (no backend, no auth, no external APIs).
# No environment variables are required for v1.
#
# If future features require backend services, add variables here, e.g.:
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=
